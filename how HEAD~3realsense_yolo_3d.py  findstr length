[33mcommit cf2978c6004ee955c38172d4ba3f611b57488988[m
Author: lumencroft <lumencroft@gmail.com>
Date:   Mon Sep 8 15:58:10 2025 +0900

    Implement ready signal-based sequential button detection: UP button -> ready signal -> 7 button -> ready signal -> repeat, and change UDP port to 5003

[1mdiff --git a/realsense_yolo_3d.py b/realsense_yolo_3d.py[m
[1mindex 4883081..39500c8 100644[m
[1m--- a/realsense_yolo_3d.py[m
[1m+++ b/realsense_yolo_3d.py[m
[36m@@ -47,12 +47,12 @@[m [mclass RealSenseYOLO3D:[m
         # UDP transmission setup[m
         self.udp_socket = socket.socket(family=socket.AF_INET, type=socket.SOCK_DGRAM)[m
         self.target_ip = "192.168.1.131"[m
[31m-        self.target_port = 5005[m
[32m+[m[32m        self.target_port = 5003[m
         [m
         # UDP reception setup (receive ready signal)[m
         self.udp_receiver = socket.socket(family=socket.AF_INET, type=socket.SOCK_DGRAM)[m
         self.receiver_ip = "0.0.0.0"  # Receive from all interfaces[m
[31m-        self.receiver_port = 5006[m
[32m+[m[32m        self.receiver_port = 5003[m
         self.udp_receiver.bind((self.receiver_ip, self.receiver_port))[m
         [m
         # BUTTON_POSITION protocol setup (referencing psock structure)[m
[36m@@ -61,10 +61,11 @@[m [mclass RealSenseYOLO3D:[m
         self.length = 16  # Changed to 16 as per specification[m
         [m
         # State management[m
[31m-        self.state = "waiting_ready"  # waiting_ready, looking_up, looking_7, completed[m
[32m+[m[32m        self.state = "waiting_ready"  # waiting_ready, looking_up, looking_7[m
         self.ready_signal_received = False[m
         self.up_button_sent = False[m
         self.button_7_sent = False[m
[32m+[m[32m        self.current_sequence = "up"  # "up" or "7" - which button to look for next[m
         [m
         # Timer for button detection transmission[m
         self.last_send_time = 0[m
[36m@@ -133,9 +134,16 @@[m [mclass RealSenseYOLO3D:[m
                     if start_byte == 'POLA' and message_id == 103 and ready == 1:[m
                         print(f"🎯 Ready signal received! (from {addr[0]}:{addr[1]})")[m
                         self.ready_signal_received = True[m
[31m-                        self.state = "looking_up"[m
[31m-                        self.up_button_sent = False[m
[31m-                        self.button_7_sent = False[m
[32m+[m[41m                        [m
[32m+[m[32m                        # Determine which button to look for based on current sequence[m
[32m+[m[32m                        if self.current_sequence == "up":[m
[32m+[m[32m                            self.state = "looking_up"[m
[32m+[m[32m                            self.up_button_sent = False[m
[32m+[m[32m                            print("🎯 Looking for UP button...")[m
[32m+[m[32m                        elif self.current_sequence == "7":[m
[32m+[m[32m                            self.state = "looking_7"[m
[32m+[m[32m                            self.button_7_sent = False[m
[32m+[m[32m                            print("🎯 Looking for 7 button...")[m
                         [m
             except Exception as e:[m
                 if self.running:  # Only print error if not normal termination[m
[36m@@ -187,8 +195,8 @@[m [mclass RealSenseYOLO3D:[m
         )[m
         [m
         print("\n🎥 RealSense + YOLO + 3D coordinate calculation started!")[m
[31m-        print("🎯 Waiting for ready signal... (receiving on UDP port 5006)")[m
[31m-        print("🎯 When ready signal received: UP button → 7 button detection sequence")[m
[32m+[m[32m        print("🎯 Waiting for ready signal... (receiving on UDP port 5003)")[m
[32m+[m[32m        print("🎯 Sequence: Ready signal → UP button → Ready signal → 7 button → Ready signal → UP button...")[m
         print(f"📡 Transmission target: {self.target_ip}:{self.target_port}")[m
         print("\n⌨️  Keyboard input:")[m
         print("   'c' key: distortion correction toggle")[m
[36m@@ -288,12 +296,14 @@[m [mclass RealSenseYOLO3D:[m
                                         [m
                                         if self.state == "looking_up":[m
                                             self.up_button_sent = True[m
[31m-                                            self.state = "looking_7"[m
[31m-                                            print("🎯 UP button transmission completed! Now looking for 7 button...")[m
[32m+[m[32m                                            self.state = "waiting_ready"[m
[32m+[m[32m                                            self.current_sequence = "7"  # Next time ready signal comes, look for 7 button[m
[32m+[m[32m                                            print("🎯 UP button transmission completed! Waiting for next ready signal to look for 7 button...")[m
                                         elif self.state == "looking_7":[m
                                             self.button_7_sent = True[m
[31m-                                            self.state = "completed"[m
[31m-                                            print("🎯 7 button transmission completed! All tasks completed!")[m
[32m+[m[32m                                            self.state = "waiting_ready"[m
[32m+[m[32m                                            self.current_sequence = "up"  # Next time ready signal comes, look for up button[m
[32m+[m[32m                                            print("🎯 7 button transmission completed! Waiting for next ready signal to look for UP button...")[m
                             else:[m
                                 text = f"{class_name}: {confidence:.2f} (No depth)"[m
                                 cv2.putText(annotated_image, text, (x1, y1-10), [m
[36m@@ -306,7 +316,8 @@[m [mclass RealSenseYOLO3D:[m
                 [m
                 # Display current state information on screen[m
                 if self.state == "waiting_ready":[m
[31m-                    status_info = "State: Waiting for ready signal..."[m
[32m+[m[32m                    next_button = "UP" if self.current_sequence == "up" else "7"[m
[32m+[m[32m                    status_info = f"State: Waiting for ready signal... (Next: {next_button} button)"[m
                     color = (0, 255, 255)  # Yellow[m
                 elif self.state == "looking_up":[m
                     status_info = "State: Looking for UP button..."[m
[36m@@ -314,9 +325,6 @@[m [mclass RealSenseYOLO3D:[m
                 elif self.state == "looking_7":[m
                     status_info = "State: Looking for 7 button..."[m
                     color = (255, 0, 0)    # Blue[m
[31m-                elif self.state == "completed":[m
[31m-                    status_info = "State: All tasks completed!"[m
[31m-                    color = (0, 255, 0)    # Green[m
                 else:[m
                     status_info = f"State: {self.state}"[m
                     color = (255, 255, 255)  # White[m
[36m@@ -361,7 +369,8 @@[m [mclass RealSenseYOLO3D:[m
                     self.state = "waiting_ready"[m
                     self.up_button_sent = False[m
                     self.button_7_sent = False[m
[31m-                    print(f"\n🔄 State reset: Waiting for ready signal...")[m
[32m+[m[32m                    self.current_sequence = "up"  # Reset to start with UP button[m
[32m+[m[32m                    print(f"\n🔄 State reset: Waiting for ready signal... (Next: UP button)")[m
                     [m
         except KeyboardInterrupt:[m
             print("\nInterrupted by user.")[m
